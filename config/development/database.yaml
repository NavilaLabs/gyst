database:
  base_uri: "sqlite:///workspaces/zeitrak/.devcontainer/database"
  databases:
    admin:
      name: zeitrak_admin
    tenant:
      name_prefix: zeitrak_tenant_
  pool:
    max_size: 20
    min_size: 5
    timeout_seconds: 30
