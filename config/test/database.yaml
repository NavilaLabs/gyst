database:
  base_uri: ${ZK_DATABASE_BASE_URI}

  databases:
    admin:
      name: test_zeitrak_admin
    tenant:
      name_prefix: test_zeitrak_tenant_

  pool:
    max_size: 20
    min_size: 5
    timeout_seconds: 30
